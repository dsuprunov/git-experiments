name: Three jobs

on:
  workflow_dispatch:

jobs:
  configure:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Configure"

  build:
    runs-on: ubuntu-latest
#    environment: env_1
    needs:
      - configure
    steps:
      - run: echo "Build"

  test:
    runs-on: ubuntu-latest
#    environment: env_1
    needs:
      - build
    steps:
      - run: echo "Test"

  deploy:
    runs-on: ubuntu-latest
#    environment: env_2
    needs:
      - configure
    steps:
      - run: echo "Deploy"