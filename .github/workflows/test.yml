name: Three jobs

on:
  workflow_dispatch:

jobs:
  configure:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Configure"

  build:
    runs-on: ubuntu-latest
#    environment: env_1
    needs:
      - configure
    steps:
      - run: echo "Build"

  deploy:
    runs-on: ubuntu-20.04
#    environment: env_2
    needs:
      - configure
    steps:
      - run: echo "Deploy"